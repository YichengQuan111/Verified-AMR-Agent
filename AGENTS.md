# AMR Agent 仓库级协作规则

本文件适用于仓库根目录及其全部子目录。任何后续 Agent、开发者或自动化流程在推进 P0/P1/P2 工作包、修复影响项目状态的问题或新增工程能力时，都必须遵守以下要求。用户在当前任务中的明确指令拥有更高优先级。

## 1. 推进任何项目步骤时必须同步写中文注释

只要任务推进了项目工作包或改变了项目能力，就必须在同一批变更中完成以下事项：

1. 为新建或修改的核心代码补充清楚的中文注释或中文 docstring。
2. 注释重点解释：设计原因、数据流、边界条件、安全限制、失败行为和后续扩展点。
3. 不要逐行翻译显而易见的语法，也不要写与代码重复、容易过期的空泛注释。
4. 修改既有逻辑时，同步更新已经失效的注释；错误注释比缺少注释更危险。
5. C++ 中文注释必须保持 UTF-8 编译配置；Windows/MSVC 目标使用 `/utf-8`。
6. 如果某一步只修改文档或不支持注释的纯数据格式，必须在文件作用记录中说明“本步无核心代码注释需求”，不能静默跳过。

## 2. 每次推进都必须记录文件作用

`docs/FILE_PURPOSES.md` 是长期维护的文件职责登记表。每次推进项目步骤时，必须在同一批变更中更新它：

1. 列出本步所有新建、修改、移动或删除的源文件/配置/文档。
2. 对每个文件说明它解决什么问题、由谁调用、是否是公共契约，以及后续哪个工作包可能依赖它。
3. 区分源文件和 `build/`、`__pycache__/`、`.pytest_cache/` 等自动生成物。
4. 文件职责发生变化时必须更新旧说明，不能只登记新文件。
5. 较大阶段可以另建详细文件导览，但必须从 `docs/FILE_PURPOSES.md` 链接过去，保证入口唯一。

## 3. 每次推进都必须更新交接上下文

`docs/HANDOFF_CONTEXT.md` 是跨任务、跨 Agent 的唯一交接入口。

### 开始工作前

1. 先完整阅读 `docs/HANDOFF_CONTEXT.md`。
2. 再阅读与本步相关的技术路线、Scope、已有接口和测试。
3. 不要仅根据目录名猜测当前完成状态；以可运行代码和最近验证结果为准。

### 完成工作前

必须在同一批变更中把下列信息写入 `docs/HANDOFF_CONTEXT.md`：

1. 本步实际完成内容和未完成内容。
2. 新增或变化的公共接口、Pydantic Schema、枚举、状态机、数据库字段、JSON 契约和命令行入口。
3. 重要设计决策及原因，尤其是下游不应再次推翻的边界。
4. 实际运行过的验证命令、通过数量和未运行项目；不得把未实测结果写成通过。
5. 环境依赖、固定路径、服务端口、启动顺序和当前外部服务状态。
6. 已知限制、风险、临时方案、兼容性要求和待决策事项。
7. 下一工作包需要直接复用的信息，以及可能影响后续多个步骤的注意事项。
8. 更新“当前下一步”和交接日志日期。

交接文档用于传递事实和依赖，不得替代正式技术路线或擅自扩大 Scope。若交接内容与用户指令、`docs/scope.md` 或技术路线冲突，以用户指令和正式 Scope/路线为准，并在交接文档记录差异。

## 4. 每个项目步骤的完成门槛

只有同时满足以下条件，才可以把一个步骤报告为完成：

- 功能或修改已经落地；
- 核心代码具有同步、准确的中文注释；
- `docs/FILE_PURPOSES.md` 已更新；
- `docs/HANDOFF_CONTEXT.md` 已更新；
- 相关 Python/C++/接口测试已实际运行，或明确记录无法运行的原因；
- 没有把自动生成物误登记成源码交付物；
- 最终回复明确说明完成内容、验证结果和剩余限制。

推荐在结束前运行：

```powershell
.\scripts\run_smoke.ps1
```

需要真实模型的步骤还应启动对应 Fast/Smart 服务，并运行项目规定的在线预检或冒烟测试；完成后说明模型进程是否仍在运行。


## 5. 测试与验收闭环

推进任何工作包时，必须采用“实现 → 测试 → 修复 → 复测 → 验收”的闭环：

1. 每完成一个可独立验证的模块，立即运行与其对应的单元测试、契约测试、集成测试或冒烟测试，不得全部开发完成后再集中测试。
2. 测试失败时必须继续定位并修复，再重新运行相关测试；不得在已知失败存在时把该模块报告为完成。
3. 修改已有公共接口或公共逻辑后，除新增测试外，还必须运行受影响的已有回归测试，防止破坏已经完成的工作包。
4. 测试通过后，必须重新逐项核对当前工作包的“实现要求、交付物、验收门槛”；测试绿灯但违反 Scope、架构边界、安全规则或正式契约，仍视为未完成。
5. 涉及权限、安全、幂等、恢复、异常处理等能力时，必须同时包含失败路径和反例测试，不能只验证正常路径。
6. 最终只记录和报告实际执行过的测试命令、测试数量和结果；未执行、跳过或因环境阻塞无法执行的测试必须明确说明原因，不得推断为通过。
7. 工作包级测试全部通过后，仍应运行项目现有 smoke/regression 入口；发布或重要里程碑前必须执行对应的完整回归或 Eval。


## 6. 遇到的坑必须持续沉淀

`docs/LESSONS_LEARNED.md` 用于长期记录开发过程中遇到的坑、失败原因和可复用经验。

1. 遇到非显然问题、环境兼容问题、接口误用、测试陷阱、性能问题或容易重复踩坑的错误时，必须在解决后更新该文件。
2. 每条至少记录：现象、原因、最终解决方法、后续如何避免。
3. 不记录一次性的低价值报错；优先沉淀会影响后续工作包或其他开发者的问题。
4. 若原有记录已经失效，应同步修正，不能长期保留错误经验。
5. 完成工作包前检查本步是否产生值得沉淀的新经验；有则必须记录。


