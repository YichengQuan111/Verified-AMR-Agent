# Infrastructure

Infrastructure assets live here as later work packages add model launch metadata and deployment helpers. PostgreSQL and Qdrant are currently defined by the root `compose.yaml`; the Windows llama.cpp services remain host processes by design.

