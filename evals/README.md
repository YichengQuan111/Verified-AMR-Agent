# Evaluations

This directory is reserved for the versioned P0 normal, failure, permission, safety, and strategy-comparison datasets. Evaluation code must never expose fault-injection controls through the normal Agent tool registry.

